import diagnostic/diagnostic

diagnostic.note("starting diagnostic demo")
diagnostic.warn("this is only a warning")
diagnostic.error_if(true, "this is a recoverable error report")
if diagnostic.require(true, "unreachable requirement failure") != 0 {
    ret 10
}

ret 0
